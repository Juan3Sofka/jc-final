import { useState } from "react"



export const ToDo= () => {
    const [task, setTask] = useState(null)
}
const handleTask = () => {
    setTask({}) //Hacer lógica para que cambie cada componente de cada task
    //Preguntar como pasar del formulario a la siguiente página
    //Pregunar como puedo hacer lo de la hora-Fecha
    //Preguntar que recomendaciones me da a la página
}  